feat: implement complete Grande Tontine module (TICKET-008, TICKET-009, TICKET-010)

TICKET-008 — Domain + Infrastructure:
- Create value objects: TontineTourStatus, PaymentStatus, DebtStatus
- Create domain models: TontineTour, TontineParticipant, TontineContribution, TontineDebt
- Create domain repository ports (4 interfaces)
- Create JPA entities matching V1 schema with proper constraints
- Create mappers implementing EntityMapper<D, E> pattern
- Create JPA adapters with inner Spring Data interfaces (port/adapter pattern)

TICKET-009 — Use cases:
- CreateTourUseCase: validates no open tour exists, min 2 participants, creates tour + participants
- RecordTontineContributionUseCase: handles 3 cases (normal contribution with debt creation, repayment with existing debt, default with sanction + possible reclassification)
- MarkBenefitedUseCase: marks a participant as having benefited
- CloseTourUseCase: closes tour only when all participants have benefited
- GetTourDebtsUseCase: fetches debts for a tour, optionally filtered by member

TICKET-010 — Controller + Templates:
- CreateTourForm: startDate, participantIds, drawOrders
- ContributionForm: contributorId, beneficiaryId, amount, sessionId
- TontineController: 6 routes (list, create GET/POST, detail, contribute GET/POST, close)
- Templates: list.html (open tour card + history), form.html (member selection + draw order), tour-detail.html (participants/debts/contributions), contribution-form.html (beneficiary selection, per-contributor amount with JS syncing)
