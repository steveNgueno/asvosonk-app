feat: implement complete Banque Annuelle module (TICKET-011, TICKET-012, TICKET-013)

TICKET-011 — Domain + Infrastructure:
- Create value object: LoanStatus (active, repaid, overdue)
- Create domain models: Saving, Loan (with factory createNew, business methods), LoanRepayment
- Create repository ports: SavingRepository, LoanRepository, LoanRepaymentRepository
- Create JPA entities matching V1 schema (saving, loan, loan_repayment)
- Create mappers (SavingMapper, LoanMapper, LoanRepaymentMapper)
- Create JPA adapters with Spring Data inner interfaces (sum queries via @Query)

TICKET-012 — Use cases:
- RecordSavingUseCase: validates amount > 0, member active, creates saving + bank cashbox credit
- CreateLoanUseCase: validates member active, has savings > 0, max 2 active loans; calculates totalDue (10% interest) and dueDate (2 months); creates bank cashbox debit
- RecordLoanRepaymentUseCase: validates loan active, records repayment, auto-closes loan when fully paid, creates bank cashbox credit
- CheckOverdueLoansUseCase: @Scheduled daily at 02:00, marks overdue loans
- GetMemberBankSummaryUseCase: returns totalSavings, activeLoans, loanHistory, eligibility

TICKET-013 — Controller + Templates:
- Forms: SavingForm (amount, operationDate), LoanForm (amount), RepaymentForm (amount)
- BankController: 5 routes (overview, member detail, save, loan, repay)
- overview.html: total savings card, active loans table with member names/remaining balance/status, member list
- member-account.html: savings total, eligibility badge, saving/loan/repay modals, active loans with repayment progress, loan/savings history
