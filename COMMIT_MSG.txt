feat: implement Reports module (TICKET-015 Java bridge + TICKET-016 Python generator)

TICKET-015 — Java side:
- ReportService: bridges to Python executable via ProcessBuilder, reads output path
  from stdout, 60-second timeout, proper error handling with exit code check
- ReportController: GET /reports (page), POST /reports/generate (PDF download)
  via ResponseEntity with Content-Disposition: attachment
- reports/index.html: 3 report type cards (session/monthly/quarterly) with
  date picker modals, info banner, auto-download message

TICKET-016 — Python report generator:
- main.py: entry point dispatching by type, prints output path to stdout
- db_config.py: PostgreSQL connection via read-only role (asvosonk_reports)
- reports/session_report.py: attendance, tontine, cashbox movements PDF
- reports/monthly_report.py: sessions, cashbox summary, sanctions, revolving fund
- reports/quarterly_report.py: aggregated quarterly overview per cashbox
- requirements.txt: psycopg2-binary, weasyprint, jinja2
- build.bat: PyInstaller one-file build script

Config: application.properties already has asvosonk.reports.exe-path and output-dir
Nav: layout.html already has Reports link with REPORT_SESSION permission
